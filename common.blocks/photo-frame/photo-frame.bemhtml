block('photo-frame')(content()(function(){

			return [

					{
					    block : 'image',
					    url : this.ctx.url ? '/' + this.ctx.url : '/images/avatar.png',
					    width : 240
					}
			
			]
		}
	)
)